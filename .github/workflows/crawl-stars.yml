name: crawl-stars-graphql

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * *'   # daily at 03:00 UTC

jobs:
  crawl:
    runs-on: self-hosted   # must be a runner with Postgres installed

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests psycopg2-binary tenacity

      - name: Wait for Postgres
        run: |
          for i in {1..30}; do pg_isready -h localhost -p 5432 -U postgres && break || sleep 1; done

      - name: Setup Postgres schema
        run: |
          psql "postgresql://postgres:root@localhost:5432/postgres" -f sql/setup_schema.sql

      - name: Crawl stars
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DB_DSN: postgresql://postgres:root@localhost:5432/postgres
        run: |
          python scripts/crawl_stars.py

      - name: Dump DB
        run: |
          pg_dump -h localhost -U postgres -d postgres -F c -f /tmp/postgres.dump

      - name: Upload DB dump
        uses: actions/upload-artifact@v4
        with:
          name: pg-dump
          path: /tmp/postgres.dump
